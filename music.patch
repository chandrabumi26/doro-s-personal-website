--- app/components/MusicPlayer.tsx
+++ app/components/MusicPlayer.tsx
@@ -10,6 +10,7 @@
   const [progress, setProgress] = useState(0);
   const [volume, setVolume] = useState(0.1); // 10% default volume
   const [isVolumeHovered, setIsVolumeHovered] = useState(false);
+  const [isAtBottom, setIsAtBottom] = useState(false);
 
   const audioRef = useRef<HTMLAudioElement | null>(null);
   const autoplayTimerRef = useRef<NodeJS.Timeout | null>(null);
@@ -58,6 +59,20 @@
     };
   }, []);
 
+  // Handle Scroll Detection for Mobile Re-positioning
+  useEffect(() => {
+    const container = document.querySelector('.scroll-snap-container');
+    if (!container) return;
+
+    const handleScroll = () => {
+      const isBottom = container.scrollHeight - container.scrollTop <= container.clientHeight + 150;
+      setIsAtBottom(isBottom);
+    };
+
+    container.addEventListener('scroll', handleScroll);
+    return () => container.removeEventListener('scroll', handleScroll);
+  }, []);
+
   // Apply volume changes
   useEffect(() => {
     if (audioRef.current) {
@@ -124,13 +139,13 @@
             animate={{ opacity: 1, y: 0, scale: 1 }}
             exit={{ opacity: 0, y: 50, scale: 0.95 }}
             transition={{ type: "spring", stiffness: 300, damping: 25 }}
-            className="fixed bottom-6 right-6 z-50 pointer-events-auto"
+            className={`fixed z-50 pointer-events-auto transition-all duration-500 ease-in-out left-1/2 -translate-x-1/2 w-[calc(100vw-3rem)] max-w-sm md:w-80 md:left-auto md:-translate-x-0 md:right-6 ${isAtBottom ? 'top-24 bottom-auto md:top-auto md:bottom-6' : 'bottom-6 top-auto'}`}
           >
             <motion.div
               whileHover={{
                 scale: 1.03,
                 boxShadow: "0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04)"
               }}
               transition={{ type: "spring", stiffness: 400, damping: 17 }}
-              className="w-72 sm:w-80 bg-cream/95 backdrop-blur-md border border-charcoal/15 rounded-2xl shadow-xl overflow-hidden flex flex-col retro-texture"
+              className="w-full bg-cream/95 backdrop-blur-md border border-charcoal/15 rounded-2xl shadow-xl overflow-hidden flex flex-col retro-texture"
             >
               {/* Progress bar */}
@@ -361,7 +376,7 @@
           onClick={() => {
             setIsVisible(true);
             isManuallyControlledRef.current = true;
           }}
-          className="fixed bottom-6 right-6 z-50 px-4 py-2 bg-cream/90 backdrop-blur-md border border-charcoal/15 rounded-full shadow-lg text-xs font-bold text-charcoal flex items-center gap-2 hover:bg-cream transition-colors cursor-pointer"
+          className={`fixed z-50 px-4 py-2 bg-cream/90 backdrop-blur-md border border-charcoal/15 rounded-full shadow-lg text-xs font-bold text-charcoal flex items-center gap-2 hover:bg-cream transition-colors cursor-pointer transition-all duration-500 ease-in-out left-1/2 -translate-x-1/2 md:left-auto md:-translate-x-0 md:right-6 ${isAtBottom ? 'top-24 bottom-auto md:top-auto md:bottom-6' : 'bottom-6 top-auto'}`}
           title="Restore music player"
         >
           <span className="w-2 h-2 rounded-full bg-tosca animate-pulse" />
